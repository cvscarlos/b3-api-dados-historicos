import path from 'node:path';

// eslint-disable-next-line unicorn/prefer-module
export const DOCS_DIR = path.resolve(__dirname, '../docs');
export const TXT_DIR = path.join(DOCS_DIR, '/b3-txt-files');
